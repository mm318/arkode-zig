name: Test Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 13 * * *' # run at UTC 1 pm

env:
  ZIG_VERSION: 0.15.2
  SLANG_VERSION: 2025.23.1

jobs:
  build-all-targets:
    strategy:
      matrix:
        include:
          - target: 'native'
            optimizaton: 'Debug'
          - target: 'native'
            optimizaton: 'ReleaseSafe'

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libvulkan-dev vulkan-tools

      - name: Install Slang compiler
        run: |
          set -euxo pipefail
          base="https://github.com/shader-slang/slang/releases/download/v${SLANG_VERSION}/slang-${SLANG_VERSION}-linux-x86_64.tar.gz"
          mkdir -p "$RUNNER_TEMP/slang"
          curl -fL ${base} -o "$RUNNER_TEMP/slang/slang.tar.gz"
          tar -xzf "$RUNNER_TEMP/slang/slang.tar.gz" -C "$RUNNER_TEMP/slang"
          slangc_path="$RUNNER_TEMP/slang/bin/slangc"
          if [ -z "$slangc_path" ]; then
            echo "slangc not found" >&2
            exit 1
          fi
          chmod +x "$slangc_path"
          echo "$(dirname "$slangc_path")" >> "$GITHUB_PATH"

      - uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Check formatting
        run:  |
          zig fmt --check .

      - name: Build Libraries
        run: |
          zig build -Doptimize=${{ matrix.optimizaton }}

      - name: Run Unit Tests
        run: |
          zig build -Doptimize=${{ matrix.optimizaton }} test --summary all

      - name: Run Examples
        run: |
          zig build -Doptimize=${{ matrix.optimizaton }} examples --summary all

      - name: Run Benchmarks
        run: |
          zig build -Doptimize=${{ matrix.optimizaton }} benchmarks --summary all
